An Image Carousel!
Keep you images in the format:
  div>(any number of images)
Everything else will be added automatically

you can change the style using carousel.css 
the height width of carousel can be changed through class "container-wrapper"

select the div containing images and pass it to the object constructor
call objectName.doccumentInit() to start the movement